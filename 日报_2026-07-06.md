# 开发日报

**日期**：2026年7月6日（星期一）
**今日定位**：演示日 & M5里程碑验收（研发计划 4.2 — 阶段5第3天 / 共19天）
**组长/汇报人**：孟之语
**组员**：于贺、甘毅、王佳杭
**汇报时间**：2026年7月6日 16:30

---

## 目录

1. [今日总览](今日总览)
2. [阶段5前两日回顾（7月4日-5日）](#阶段5前两日回顾7月4日-5日)
3. [晨会记录](#晨会记录)
4. [组员个人进度汇报](#组员个人进度汇报)
   - 4.1 孟之语（组长/核心算法负责人）
   - 4.2 于贺（接口与数据负责人）
   - 4.3 甘毅（内容组织负责人）
   - 4.4 王佳杭（评测与展示负责人）
5. [今日核心工作详解](#今日核心工作详解)
   - 5.1 ChromaDB 异步初始化与非阻塞启动
   - 5.2 策略预设系统（RAG / 训练 / 摘要三模式）
   - 5.3 父子块（Parent-Child Chunk）架构
   - 5.4 前端重构与演示页面
   - 5.5 分段引擎增强与 CLI 工具
   - 5.6 服务稳定性与错误处理
   - 5.7 测试修复与回归验证
   - 5.8 演示材料准备
6. [问题与解决方案](#问题与解决方案)
7. [晚间同步会记录](#晚间同步会记录21302200)
8. [组长总结](#组长总结)
9. [关键决策记录](#关键决策记录)
10. [明日计划（7月7日 文档日）](#明日计划7月7日文档日)
11. [项目整体进度追踪](#项目整体进度追踪)
12. [任务状态表](#任务状态表)
13. [里程碑验收状态](#里程碑验收状态)
14. [阶段5完整工作总结](#阶段5完整工作总结)
15. [M5验收报告](#m5验收报告)
16. [技术报告大纲与分工确认](#技术报告大纲与分工确认)
17. [演示脚本与演示场景](#演示脚本与演示场景)
18. [风险评估与缓冲消耗](#风险评估与缓冲消耗)
19. [附录：关键代码与数据](#附录关键代码与数据)

---

## 今日总览

| 项目 | 内容 |
|------|------|
| **日期** | 2026年7月6日（星期一） |
| **阶段** | 阶段5 — 集成测试、前端/报告、失败案例（第3天 / 共3天） |
| **今日定位** | 演示日：准备演示页面/HTML报告，展示chunk、回链、指标和badcase，完成M5验收 |
| **里程碑** | M5 — 验收候选版本 |
| **全组状态** | 正常，全员在岗，M5验收通过 |
| **今日计划任务** | 10项，完成10项 |
| **累计全组工时** | 约24小时 |
| **阶段5累计进度** | 100%（集成修复日 + 测试日 + 演示日 全部完成） |
| **上次里程碑** | M4 — 能输出对比实验结果（7月3日验收通过） |
| **本次里程碑** | M5 — 集成修复完成 + 演示可用（7月6日验收通过） |

### 今日在研发计划中的位置

根据研发计划 4.2 节每日计划，7月6日的定位为：

> **演示日**：准备演示页面或Markdown/HTML 报告，展示chunk、回链、指标和 badcase
> **负责人**：王佳杭、甘毅
> **产出**：演示材料初版
> **里程碑**：M5 — 服务、评测、演示材料完成候选版本

今日是阶段5（7月4日-7月6日）的第3天，也是最后一天。阶段5的三天行程：

| 日期 | 日程定位 | 核心任务 | 负责人 | 状态 |
|------|---------|---------|--------|------|
| 7月4日 | 集成修复日 | FastAPI 联调、命令行脚本、日志、配置、异常处理 | 于贺、全员 | 已完成 |
| 7月5日 | 测试日 | 单元测试、接口测试、样例回归、人工抽样 | 全员 | 已完成 |
| 7月6日 | 演示日 | 演示页面/HTML报告、前端完善、M5验收 | 王佳杭、甘毅 | 已完成 |

### 今日完成的核心交付

| 编号 | 交付物 | 说明 | 状态 |
|------|--------|------|------|
| D-1 | ChromaDB 异步初始化 | 后台线程初始化，不阻塞FastAPI启动，向量操作失败静默降级 | 已完成 |
| D-2 | 策略预设系统 | RAG检索/训练语料/文档摘要 三种预设，一键切换分段策略 | 已完成 |
| D-3 | 父子块架构 | Parent-Child Chunk 数据结构 + SegmentConfig.enable_parent_child | 已完成 |
| D-4 | 前端重构 | ChunkList.vue 精简580行至核心展示逻辑，ConfigPanel 预设选择器 | 已完成 |
| D-5 | 测试修复 | 2个失败测试修复，27/27测试全部通过 | 已完成 |
| D-6 | 演示脚本 | 5个演示场景的完整操作流程 + 预期结果 | 已完成 |
| D-7 | 分段引擎 CLI | segment_file.py 支持 --preset、--parent-child、--output-json | 已完成 |
| D-8 | M5 里程碑验收 | 全部验收标准通过 | 已完成 |

---

## 阶段5前两日回顾（7月4日-5日）

### 7月4日 集成修复日

根据研发计划，7月4日的定位为集成修复日：FastAPI 全功能联调、命令行脚本与工具链补齐、日志系统与配置管理、中间件与异常处理。负责人为于贺和全员。

**孟之语**完成工作：

1. **SegmentConfig 预设策略设计与实现**：在 models.py 中新增 `for_preset()` 类方法和 `enable_parent_child` 字段。预设策略是研发计划中"进阶方向"提到的"策略切换"需求的具体实现——用户无需手动调整6个分段参数，只需选择"RAG检索优化""训练语料生产"或"文档摘要"三个预设之一，系统自动配置最优参数组合。

2. **API 层预设支持**：在 routes.py 的 upload_and_segment 端点新增 `preset` 和 `parent_child` 两个可选参数。当用户指定 preset 时，优先使用预设策略的参数覆盖自适应参数。在 /api/strategies 端点新增 presets 列表字段，返回三个预设的完整描述。

3. **分段引擎集成**：在 segmenter.py 中完善了 preset 的处理逻辑和 parent-child chunk 的生成链路。

**于贺**完成工作：

1. **ChromaDB 异步初始化**：将 chroma_store.py 中的 `initialize_chroma()` 从同步阻塞改为后台线程异步初始化。这是针对7月3日发现的"ChromaDB 首次初始化可能超时"问题的根治方案。改动要点：
   - 使用 `threading.Thread(daemon=True)` 在后台初始化 ChromaDB
   - 新增 `_chroma_ready` 全局标志位和 `_chroma_init_lock` 线程锁
   - 所有向量操作（upsert/query/delete）在 `_chroma_ready=False` 时静默跳过
   - 新增 `_ensure_collection()` 作为安全获取 collection 的统一入口
   - 服务启动时间从 ~30s（含 ChromaDB 模型下载）降至 ~2s（仅 FastAPI + SQLite）

2. **服务启动流程简化**：在 main.py 中去掉了 startup 事件中冗余的 try/except，改为直接调用初始化函数。理由：初始化函数内部已有完善的错误处理（ChromaDB 失败不影响服务），外层不需要额外包裹。

3. **rag_store/service.py 重构**：将 ingest_document 函数扩展为支持 preset 和 parent_child 参数。preset 不为空时，使用 `SegmentConfig.for_preset(preset)` 替代 `SegmentConfig.auto(total_chars)`。

**甘毅**完成工作：

1. **API 错误处理标准化**：统一了所有11个 API 端点的错误响应格式，确保每个端点返回一致的 `{"error": "ERROR_CODE", "message": "...", "detail": "..."}` 结构。

2. **内容组织边界测试补充**：对 RuleBasedTagger 的三个核心方法增加了中英混合文本、纯数字文本、HTML标签污染文本的边界测试。

**王佳杭**完成工作：

1. **前端 ConfigPanel 预设选择器**：在分段配置面板中新增预设策略下拉选择器，用户可在"自定义参数"和三个预设之间切换。选择预设时，参数输入框自动填充并置灰（不可编辑）。

2. **前端 ChunkList 重构启动**：开始精简 ChunkList.vue（当时580行），目标是将展示逻辑模块化，提取独立的 ChunkCard、TagCapsule、EntityBadge 等子组件。

**7月4日全天总结**：

| 成员 | 主要产出 | 工时 |
|------|---------|------|
| 孟之语 | 预设策略设计+实现、API扩展、分段引擎集成 | 约6h |
| 于贺 | ChromaDB异步初始化、启动流程简化、rag_store重构 | 约5.5h |
| 甘毅 | API错误处理标准化、内容组织边界测试 | 约5h |
| 王佳杭 | 前端预设选择器、ChunkList重构启动 | 约5h |

### 7月5日 测试日

根据研发计划，7月5日的定位为测试日：单元测试、接口测试、样例回归、人工抽样检查摘要和标签。负责人为全员。

**孟之语**完成工作：

1. **全量回归测试**：运行全部27个后端测试，发现2个失败用例。逐一分析根因并修复：
   - `test_model_settings.py::test_initialize_and_update_keep_quality_evaluator`：mock 路径 `backend.app.services.model_settings` 在7月3日重构（model_settings → core/）后失效。修复：更新 mock 路径为 `backend.app.core.model_settings`。
   - `test_segmenting.py::test_docx_sample_keeps_source_refs_complete`：chunk 数量断言 `<= 20` 在新增 parent-child chunk 后变为22，超出旧断言。修复：将断言更新为 `<= 25` 以兼容父子块模式。

2. **接口测试**：使用 curl 对全部11个端点进行回归测试，覆盖正常请求、边界请求（空文件、超大文件、缺失必填字段）、错误请求（不支持的格式、无效doc_id）三类场景。

**于贺**完成工作：

1. **ChromaDB 降级验证**：模拟 ChromaDB 初始化失败的场景（删除 chromadb 包），验证服务仍可正常启动、分段不受影响、向量检索返回空结果且不报错。

2. **长文档分段性能测试**：对 Apple 10-K（88K行）和 NVIDIA 10-K（103K行）进行完整分段性能测试，记录耗时和内存占用。

3. **RAG Store 集成测试**：验证 SQLite 元数据存储 + ChromaDB 向量存储的联合读写一致性。

**甘毅**完成工作：

1. **人工抽样检查**：从3份文档中各随机抽取3个 chunk（共9个），人工检查标签质量、摘要忠实度和实体识别准确性。抽样结果：标签准确率7/9、摘要忠实度9/9、实体精确率8/9。总体抽样通过率88.9%。

2. **LLM模式回归测试**：设置 DeepSeek API Key 后，验证 LLM 增强模式仍正常工作（标签、摘要、实体三项均可生成）。验证 LLM 不可用时的降级行为：取消 API Key 后，自动回退到规则模式，不影响服务可用性。

**王佳杭**完成工作：

1. **前端 ChunkList 重构完成**：从580行精简至约300行核心展示逻辑。提取了 ChunkCard、TagCapsule、EntityBadge、SourceRefPopover 四个子组件。重构后渲染性能提升（减少不必要的响应式依赖），代码可维护性显著改善。

2. **前端错误处理增强**：为所有 API 调用增加了统一的错误拦截器，在 API 返回错误时显示用户友好的提示信息而非控制台报错。

3. **演示页面布局设计**：确定了演示页面的四大展示区域：分段结果区、检索评测区、内容组织区、指标汇总区。

**7月5日全天总结**：

| 成员 | 主要产出 | 工时 |
|------|---------|------|
| 孟之语 | 测试修复2个、11端点回归测试 | 约5h |
| 于贺 | ChromaDB降级验证、长文档性能测试、RAG Store集成测试 | 约5.5h |
| 甘毅 | 人工抽样9 chunk、LLM回归测试 | 约5h |
| 王佳杭 | 前端重构完成、错误处理、演示布局设计 | 约5.5h |

### 遗留问题收敛（7月4日-5日）

| 编号 | 问题描述 | 发现日期 | 状态 |
|------|---------|---------|------|
| P-7.3-1 | EmbeddingStore.add_chunks() 多次调用累积 | 7月3日 | 已解决：新增 clear_document() 方法 |
| P-7.3-2 | 超长文档分段耗时过长 | 7月3日 | 已缓解：新增 --cache 参数，支持分段结果缓存到 .pkl |
| P-7.3-3 | org_suffix 实体召回率偏低 | 7月3日 | 已修复：后缀词表扩充至18项 |
| P-7.3-4 | metric 正则未覆盖英文全称 | 7月3日 | 已修复：新增 Accuracy、F1-score、ROUGE-L、BLEU |
| P-7.4-1 | ChromaDB 初始化阻塞启动 | 7月4日 | 已解决：异步初始化 + 静默降级 |
| P-7.5-1 | test_model_settings mock 路径过期 | 7月5日 | 已修复 |
| P-7.5-2 | test_segmenting chunk_count 断言过严 | 7月5日 | 已修复 |

---

## 晨会记录（09:30-09:55）

### 昨日（7月5日）回顾

**孟之语汇报**：

- 昨日完成全量回归测试，27个测试中2个失败，根因已定位并修复（mock路径过期 + chunk_count断言过严）。修复后27/27全部通过。
- 对11个API端点进行了回归测试，覆盖正常/边界/错误三类场景。除 ChromaDB 相关端点（/api/query 语义模式）在 ChromaDB 未就绪时返回空结果外，其余端点均正常。
- 确认了 M5 验收标准草案，今日晨会讨论确认。

**于贺汇报**：

- 昨日验证了 ChromaDB 降级机制：模拟 chromadb 包缺失的场景，服务正常启动，分段功能不受影响，向量检索静默返回空结果，整体无报错。
- 长文档性能测试结果：Apple 10-K（88K行）分段耗时约2分15秒（首次），缓存命中后约0.3秒。NVIDIA 10-K（103K行）分段耗时约2分50秒（首次），缓存命中后约0.4秒。
- RAG Store 集成测试通过：SQLite 元数据与 ChromaDB 向量数据的增删改查一致性验证无异常。

**甘毅汇报**：

- 昨日完成人工抽样检查：9个 chunk 的标签/摘要/实体质量抽样，总体通过率88.9%。1个标签漏检（调研报告中的英文专有名词"Unstructured.io"被 jieba 拆散），1个实体误检（"XX团队"中的"团队"未在后缀词表中，但实际不是机构名）。这两个问题已在7月5日晚间修复（扩充后缀词表、增加英文专有名词白名单）。
- LLM 模式回归测试通过：DeepSeek API 正常时标签/摘要/实体生成正常；API Key 取消后自动降级为规则模式，服务不中断。

**王佳杭汇报**：

- 昨日完成前端 ChunkList 重构：从580行精简至约300行，提取4个子组件。渲染性能改善，代码可读性提升。
- 前端错误处理增强：统一 API 错误拦截器，用户看到友好提示而非控制台报错。
- 演示页面布局设计完成，四大展示区域确定。

### 昨日全天工作总结

| 成员 | 主要产出 | 工时 |
|------|---------|------|
| 孟之语 | 测试修复2个、11端点回归、M5验收标准草案 | 约5h |
| 于贺 | ChromaDB降级验证、长文档性能测试、RAG Store集成测试 | 约5.5h |
| 甘毅 | 人工抽样9 chunk、LLM回归、后缀词表扩充 | 约5.5h |
| 王佳杭 | 前端重构完成、错误处理、演示布局设计 | 约5.5h |

### 今日任务分配

| 编号 | 任务 | 负责人 | 预计工时 | 优先级 | 依赖 |
|------|------|--------|---------|--------|------|
| T-11.1 | M5 验收：10项 checklist 逐项检查，确认候选版本可运行 | 孟之语 | 1.5h | P0 | — |
| T-11.2 | 演示脚本编写：5个演示场景的完整操作流程 | 王佳杭 | 2h | P0 | T-11.1 |
| T-11.3 | 前端演示页面最终完善：四大区域数据对接、样式统一 | 王佳杭 | 2h | P0 | T-11.2 |
| T-11.4 | 分段引擎 CLI 完善：--preset、--parent-child、--output-json | 孟之语 | 1.5h | P1 | — |
| T-11.5 | 全量回归测试最终确认：27个测试 + 11端点 + 3文档端到端 | 于贺 | 2h | P0 | — |
| T-11.6 | 服务稳定性验证：连续运行30分钟 + 并发请求测试 | 于贺 | 1.5h | P1 | — |
| T-11.7 | 演示文档编写：README 中的演示指南章节 | 甘毅 | 2h | P1 | — |
| T-11.8 | 演示预演：全组参与，模拟正式演示流程 | 全员 | 1h | P0 | T-11.2, T-11.3 |
| T-11.9 | 技术报告大纲最终确认 + 各章节负责人分配 | 孟之语 | 0.5h | P1 | — |
| T-11.10 | 编写今日日报 | 孟之语 | 1h | P2 | — |

### 晨会关键决策

**决策 1 — M5 验收标准确认**

背景：研发计划中 M5 的验收标准为服务、评测、演示材料完成候选版本。需要将其细化为可逐项检查的验收清单。

决议：M5 验收标准确认为以下 10 项：

| # | 验收项 | 具体标准 | 验证方法 |
|---|--------|---------|---------|
| 1 | 服务可启动 | 一条命令启动，/health 正常响应 | `python -m uvicorn backend.app.main:app` |
| 2 | 11端点可用 | 所有端点有正常请求/响应，错误处理完善 | curl 逐一测试 |
| 3 | 分段全链路 | 4种格式（txt/md/docx/pdf）文档上传→分段→入库→检索 | 上传4份测试文档 |
| 4 | 预设策略可用 | RAG/训练/摘要三个预设可正常切换并影响分段结果 | 同一文档用三个预设分别分段 |
| 5 | ChromaDB 降级 | ChromaDB 不可用时服务不崩溃，向量检索返回空 | 模拟 chromadb 缺失 |
| 6 | 27个测试全通 | `pytest backend/tests/ -v` 全部通过 | 运行测试套件 |
| 7 | 前端演示可用 | 分段结果、检索评测、内容组织、指标汇总四大区域正常渲染 | 浏览器访问 |
| 8 | 评测可运行 | eval_rag.py + eval_report.py 可正常运行，输出三策略对比 | 运行评测脚本 |
| 9 | 演示脚本完备 | 5个演示场景的操作流程文档清晰可执行 | 全组预演 |
| 10 | 代码已提交 | 所有改动已 commit，分支干净 | `git status` |

**决策 2 — 父子块架构的定位**

背景：7月4日新增了 parent-child chunk 数据结构和 SegmentConfig.enable_parent_child 开关。需要明确这个功能的定位：是必交付内容还是进阶功能。

讨论过程：
- 孟之语提出：研发计划中将父子块列为预估完成内容（时间充足时实现），不作为必交付。当前我们时间不算充裕，但架构已经搭好，数据结构已定义，只需确认是否要在演示中展示。
- 于贺认为：父子块的数据结构（parent_chunk_id、child_chunk_ids）已经在 schema 中定义，API 响应中会返回这些字段。即使不启用父子分段（enable_parent_child=False），字段为空也不影响使用。保留这个架构是好的，为后续扩展留了接口。
- 王佳杭补充：前端 ChunkList 已经支持展示父子关系（通过缩进和连线），如果演示中有父子块的样例数据，展示效果会很好。

决议：父子块作为进阶功能。数据结构保留在 schema 中，SegmentConfig.enable_parent_child 默认 False。

**决策 3 — 演示场景设计**

背景：演示日是阶段5的最后一天，需要准备可供验收使用的演示材料。演示需要覆盖智能分段系统的核心能力。

决议：设计5个演示场景，按由浅入深的顺序排列：
1. 基础分段演示：上传文档 → 查看分段结果 → 检查 chunk 元数据
2. 预设策略演示：同一文档使用 RAG/训练/摘要 三个预设，对比分段效果
3. 检索评测演示：运行三策略对比评测 → 展示 Recall@5 等指标
4. 内容组织演示：查看标签/摘要/实体 → LLM 增强与规则降级切换
5. 鲁棒性演示：ChromaDB 不可用时的降级行为、错误处理

每个场景约 2-3 分钟，总计 15 分钟左右。王佳杭负责编写详细脚本。

**决策 4 — 阶段6（文档+报告）的工作节奏**

背景：阶段5今天完成后，明天（7月7日）进入阶段6（文档+报告+冻结）。距离7月10日最终提交还有4天。

决议：
- 7月7日（文档日）：孟之语负责 README 和接口文档；甘毅负责数据格式说明；于贺负责部署说明
- 7月8日（报告日）：孟之语+王佳杭负责技术报告第1/2/4/7章；甘毅+孟之语负责第3章；于贺负责第6章
- 7月9日（冻结日）：全员交叉审阅 + 演示彩排 + 代码/数据/报告冻结
- 7月10日（提交日）：最终提交 + 现场演示

---

## 组员个人进度汇报

### 孟之语（组长 / 核心算法负责人）

**今日完成**：
1. [T-11.1] M5 里程碑验收：10项 checklist 逐项检查，全部通过
2. [T-11.4] 分段引擎 CLI 完善：segment_file.py 新增 --preset、--parent-child、--output-json 参数
3. [T-11.8] 参与演示预演，确认5个演示场景的操作流程
4. [T-11.9] 技术报告大纲最终确认 + 各章节负责人分配
5. [T-11.10] 编写今日日报
6. 确认 SegmentConfig.for_preset() 三个预设的参数合理性
7. 晚间同步会向全组汇报 M5 验收结果

**实际工时**：约 6 小时

**关键产出详情**：

**产出 1 — M5 验收报告**

详见第十五节"M5验收报告"。10/10 验收项全部通过。

**产出 2 — segment_file.py CLI 增强**

为 CLI 分段工具新增了三个命令行参数：

```
usage: segment_file.py [-h] [--min MIN] [--target TARGET] [--max MAX]
                       [--overlap OVERLAP] [--threshold THRESHOLD]
                       [--preset {rag,training,summary}]
                       [--parent-child] [--output-json OUTPUT_JSON]
                       [--cache]
                       file_path

位置参数:
  file_path             要分段的文件路径（支持 txt/md/docx/pdf）

可选参数:
  --min MIN             最小 chunk 字符数
  --target TARGET       目标 chunk 字符数
  --max MAX             最大 chunk 字符数
  --overlap OVERLAP     相邻 chunk 重叠句子数
  --threshold THRESHOLD 语义边界余弦相似度阈值
  --preset {rag,training,summary}
                        预设策略（覆盖手动参数）
  --parent-child        启用父子块分段
  --output-json OUTPUT_JSON
                        输出 JSON 文件路径
  --cache               缓存分段结果（避免重复分段）
```

使用示例：

```bash
# 使用 RAG 预设分段
python scripts/segment_file.py assets/title.md --preset rag

# 使用训练预设 + 父子块 + 输出 JSON
python scripts/segment_file.py assets/title.md --preset training --parent-child --output-json outputs/chunks.json

# 使用自定义参数 + 缓存
python scripts/segment_file.py assets/survey.docx --target 650 --overlap 2 --cache
```

**产出 3 — 技术报告大纲与分工**

详见第十六节"技术报告大纲与分工确认"。

**遇到的阻塞**：
- 无重大阻塞。segment_file.py 的参数解析逻辑在加入 --preset 后需要注意与手动参数的优先级处理：--preset 指定的预设参数优先，但如果用户同时指定了 --preset 和 --target，应该报错提示二选一，而不是静默忽略其中一个。已实现参数冲突检测。

---

### 于贺（接口与数据负责人）

**今日完成**：
1. [T-11.5] 全量回归测试最终确认：27个测试全部通过，11端点正常，3文档端到端验证通过
2. [T-11.6] 服务稳定性验证：30分钟连续运行无崩溃，并发10请求无异常
3. 验证了 ChromaDB 异步初始化在生产环境的表现：首次启动时 ChromaDB 在后台线程初始化约 12 秒，期间所有分段和 SQLite 查询正常
4. 确认了 RAG Store 在 ChromaDB 未就绪时的降级行为：向量检索返回空结果（而非报错），SQLite 元数据查询正常
5. 编写了部署说明文档初稿

**实际工时**：约 5.5 小时

**关键产出详情**：

**产出 1 — 全量回归测试报告**

| 测试类别 | 测试项 | 通过 | 失败 | 通过率 |
|---------|--------|------|------|--------|
| 单元测试 | 27个 | 27 | 0 | 100% |
| API 端点 | 11个 | 11 | 0 | 100% |
| 端到端 | 3文档 × 全链路 | 3 | 0 | 100% |
| 格式支持 | txt/md/docx/pdf | 4 | 0 | 100% |
| 预设策略 | RAG/训练/摘要 | 3 | 0 | 100% |
| ChromaDB 降级 | 模拟缺失 | 通过 | — | — |
| **合计** | — | **48** | **0** | **100%** |

**端到端测试详情**：

| # | 文档 | 格式 | block数 | chunk数 | 分段耗时 | 组织耗时 | 入库耗时 | 检索正常 |
|---|------|------|---------|---------|---------|---------|---------|---------|
| 1 | title.md | md | 24 | 5 | 0.3s | 0.2s | 0.1s | OK |
| 2 | 开题报告.docx | docx | 103 | 16 | 1.2s | 0.8s | 0.4s | OK |
| 3 | 调研报告.docx | docx | 172 | 38 | 2.1s | 1.5s | 0.6s | OK |

**产出 2 — 服务稳定性验证**

- 30分钟连续运行：服务进程无崩溃，内存占用稳定在 ~350MB（含 MiniLM 模型 ~120MB）
- 并发测试：使用 Apache Bench（ab）发送 10 并发共 100 请求到 /health 端点，全部 200 响应，平均响应时间 12ms，P99 响应时间 45ms
- ChromaDB 延迟初始化：服务启动后 ChromaDB 在后台线程中初始化，约 12 秒后 _chroma_ready 变为 True。期间进行了 3 次分段操作（不依赖 ChromaDB），全部正常完成。ChromaDB 就绪后进行向量检索，结果正确返回

**产出 3 — 部署说明文档初稿**

编写了 docs/deployment.md 的初稿，包含：
- 环境要求（Python 3.10+、Node.js 18+）
- 后端安装步骤（pip install -r requirements.txt）
- 前端安装步骤（npm install）
- 启动命令（后端: uvicorn + 前端: npm run dev）
- 配置说明（.env 文件中的 API Key、模型选择）
- 常见问题（端口占用、模型下载、ChromaDB 超时）

**遇到的阻塞**：
- 无重大阻塞。并发测试时发现 ChromaDB 的 PersistentClient 在高并发写入时有偶发的文件锁冲突（约 1/50 的概率）。原因是 SQLite（ChromaDB 底层使用 SQLite）的写锁机制在并发场景下可能触发 database is locked 错误。解决方案：在 RAG Store 的写入操作外层增加了 retry 逻辑（最多重试 3 次，间隔 0.5s/1s/2s 递增等待）。已在 service.py 中实现。

---

### 甘毅（内容组织负责人）

**今日完成**：
1. [T-11.7] 演示文档编写：README 中的演示指南章节，包含5个演示场景的操作步骤
2. [T-11.8] 参与演示预演，负责内容组织场景（场景4）的演示操作
3. 更新了内容组织质量基线数据：将7月5日扩充的后缀词表和 metric 正则在人工抽样中重新验证
4. 编写了内容组织模块的开发者文档（docs/organizer.md）：RuleBasedTagger 使用说明、LLM 模式配置、降级机制说明
5. 协助于贺完成了人工抽样 9 chunk 的质量评估详细记录

**实际工时**：约 5 小时

**关键产出详情**：

**产出 1 — 演示指南（README 新增章节）**

在 README.md 中新增了"演示指南"章节，包含5个演示场景的逐步操作说明：

**场景1：基础分段演示**
```bash
# Step 1: 启动服务
python -m uvicorn backend.app.main:app --reload

# Step 2: 上传文档
curl -X POST http://localhost:8000/api/segment/upload \
  -F "file=@assets/title.md" \
  -F "keyword_strategy=tfidf"

# Step 3: 查看分段结果
curl http://localhost:8000/api/chunks/all

# 预期结果：含完整元数据的 chunk 列表
```

**场景2：预设策略演示**
```bash
# 同一文档用三个预设分别分段
curl -X POST http://localhost:8000/api/segment/upload \
  -F "file=@assets/survey.docx" -F "preset=rag"
curl -X POST http://localhost:8000/api/segment/upload \
  -F "file=@assets/survey.docx" -F "preset=training"
curl -X POST http://localhost:8000/api/segment/upload \
  -F "file=@assets/survey.docx" -F "preset=summary"

# 对比 chunk 数量：RAG（~38 chunks）vs Training（~18 chunks）vs Summary（~8 chunks）
```

**场景3：检索评测演示**
```bash
python scripts/eval_rag.py
# 输出三策略对比指标表
```

**场景4：内容组织演示**
```bash
curl -X POST http://localhost:8000/api/organize \
  -H "Content-Type: application/json" \
  -d '{"doc_id": "my_doc", "chunks": [...]}'
# 检查每个 chunk 的 tags/summary/entity_labels
```

**场景5：鲁棒性演示**
```bash
# 取消 API Key 环境变量
unset OPENAI_API_KEY
# 重新调用 /api/organize
# 预期：自动降级为规则模式，服务不中断
```

**产出 2 — 内容组织模块开发者文档**

编写了 `docs/organizer.md`，内容超过 300 行，涵盖：
- RuleBasedTagger 三个核心方法的使用说明和参数说明
- LLM 模式配置（支持的模型：DeepSeek/OpenAI）
- 降级机制详解（LLM → 规则的自动降级链路）
- 自定义停用词表和后缀词表的方法
- 性能基准（规则模式 ~0.15s/chunk、LLM 模式 ~1.2s/chunk）

**产出 3 — 后处理修复验证**

将7月5日修复的两个问题重新验证：

| 问题 | 修复内容 | 修复前 | 修复后 |
|------|---------|--------|--------|
| org_suffix 召回率低 | 后缀词表扩充至18项（+5项：团队、小组、工作组、联盟、协会） | 72.2% | 88.9% |
| metric 英文全称不覆盖 | 正则新增 Accuracy、F1-score、ROUGE-L、BLEU | 85.7% | 95.2% |

重新运行人工抽样9 chunk，实体精确率从95.9%提升至96.8%，召回率从85.5%提升至93.0%。

**遇到的阻塞**：
- 无重大阻塞。编写演示指南时需要在"简洁"和"完整"之间平衡。最终决定以可执行的命令为主，减少冗余的文字描述，让验收者能够直接复制粘贴命令运行。

---

### 王佳杭（评测与展示负责人）

**今日完成**：
1. [T-11.2] 演示脚本编写：5个演示场景的完整操作流程（含操作步骤、预期输出、讲解要点）
2. [T-11.3] 前端演示页面最终完善：四大区域数据对接完成，样式统一
3. [T-11.8] 参与演示预演，负责前端展示部分和检索评测场景
4. 确认前端与新 API 端点（strategies/presets/organize/evaluate）的数据对接
5. 更新了 eval_report.py 的输出格式：增加预设策略维度的评测对比
6. 准备了演示用的示例数据：3份核心文档 + 2份 benchmark 文档的分段结果截图

**实际工时**：约 6 小时

**关键产出详情**：

**产出 1 — 演示脚本（完整版）**

编写了约 800 行的演示脚本，包含 5 个演示场景的详细操作说明：

*场景1：基础分段演示（3分钟）*
```
操作：
1. 打开前端页面 http://localhost:5173
2. 上传 assets/title.md
3. 展示分段结果列表
4. 点击任意 chunk 查看详情

讲解要点：
- 每个 chunk 包含完整元数据（标签、摘要、实体、标题路径、回链）
- 标题边界优先策略：chunk 边界与文档标题对齐
- 不破句：所有 chunk 以完整句子结尾
- source_refs 可以追溯到原文位置
```

*场景2：预设策略演示（3分钟）*
```
操作：
1. 上传 assets/调研报告.docx
2. 分别使用"RAG检索优化""训练语料生产""文档摘要"三个预设分段
3. 对比三个预设的 chunk 数量和平均长度

讲解要点：
- RAG 预设：短 chunk + overlap → 精准命中
- 训练预设：长 chunk + 无 overlap → 语义完整
- 摘要预设：按章节分段 → 结构完整
- 同一篇文档，不同场景，不同分段策略
```

*场景3：检索评测演示（4分钟）*
```
操作：
1. 运行 python scripts/eval_rag.py
2. 展示终端输出的三策略对比指标表
3. 运行 python scripts/eval_report.py
4. 打开生成的 data/outputs/evaluation_report.md

讲解要点：
- 三策略对比：Smart vs Heading vs Fixed
- Recall@5 +18.5% 超过 +10% 目标
- 结构信息贡献 +8.9% + 语义边界增量 +8.8% = 总提升
- Badcase 分析：4个 badcase 的失败原因
```

*场景4：内容组织演示（3分钟）*
```
操作：
1. 调用 /api/organize 端点
2. 展示每个 chunk 的标签/摘要/实体
3. 设置 API Key 后，展示 LLM 增强的标签质量
4. 取消 API Key，展示自动降级为规则模式

讲解要点：
- 抽取式摘要忠实度 100%
- LLM → 规则的自动降级
- 标签准确率 0.70（规则）/ ~0.98（LLM）
```

*场景5：鲁棒性演示（2分钟）*
```
操作：
1. 展示 ChromaDB 未就绪时的启动日志
2. 执行分段操作（正常完成）
3. 执行向量检索（静默返回空，不报错）
4. 展示错误处理：无效文件格式、空文件、超大文件

讲解要点：
- ChromaDB 异步初始化不阻塞服务启动
- 向量存储不可用时，分段功能不受影响
- API 错误响应格式统一
```

**产出 2 — 前端演示页面完善**

在前端进行了以下增强：
- ConfigPanel：预设策略下拉选择器（RAG/训练/摘要/自定义），选择预设时参数自动填充且锁定
- ChunkList：父子 chunk 缩进展示（enable_parent_child=True 时生效）
- MetricPanel：指标汇总区域，展示 Smart vs Heading vs Fixed 的6项 IR 指标对比
- RetrievalPanel：检索查询面板，支持关键词搜索和语义搜索切换
- 整体样式统一：使用一致的配色方案（标题 #1a1a2e、正文 #333、标签胶囊 #e8f4f8、分数 #27ae60）

**产出 3 — 演示预演反馈的改进项**

全组预演中发现并修复了以下问题：
- 预设策略切换时，前端参数输入框的 disabled 状态未及时更新 → 已修复（增加 watch 监听）
- ChunkList 在 chunk 数量>30时滚动卡顿 → 已修复（虚拟滚动优化）
- 演示中 ChromaDB 初始化慢（12s）导致场景5的等待时间过长 → 已优化（演示前预先启动服务，让 ChromaDB 在后台完成初始化）

**遇到的阻塞**：
- 无重大阻塞。ChunkList 虚拟滚动优化的实现比预期复杂（涉及滚动位置计算和动态高度估算），实际耗时约 1h，超出计划 0.5h。通过使用 Vue 的 `v-memo` 指令和 CSS `content-visibility: auto` 实现了简化版的性能优化，避免了引入完整的虚拟滚动库。

---

## 今日核心工作详解

###  ChromaDB 异步初始化与非阻塞启动

这是阶段5最具影响力的架构改进。在7月3日之前，ChromaDB 的初始化是同步阻塞的：服务启动时调用 `initialize_chroma()`，该函数尝试连接 ChromaDB 并加载 embedding 模型，这个过程可能耗时 10-30 秒（首次下载模型时更长），期间 FastAPI 无法响应任何请求。

**问题回顾**：

ChromaDB 的初始化存在三类不确定性：
1. 模型下载：首次运行时需要从 Hugging Face 下载 embedding 模型（~120MB），网络不稳定时可能超时
2. 依赖安装：chromadb 包体积大（~200MB），在某些环境中安装可能失败
3. 版本兼容：chromadb 与 onnxruntime、sqlite3 等底层依赖存在版本兼容问题

在7月3日之前，这三类问题中的任何一个都会导致整个服务启动失败——即使分段功能完全不依赖 ChromaDB。

**解决方案架构**：

```
FastAPI 启动
  ├── initialize_model_settings()  ← 同步，必须成功（< 0.1s）
  ├── initialize_rag_store()
  │   ├── initialize_sqlite()      ← 同步，必须成功（< 0.1s）
  │   └── initialize_chroma()      ← 异步，后台线程（10-30s）
  │       └── threading.Thread(daemon=True)
  │           ├── PersistentClient(path)
  │           ├── get_or_create_collection()
  │           └── _chroma_ready = True
  └── app.include_router(router)    ← 同步（< 0.1s）

服务就绪（~2s）

向量操作：
  upsert_chunks → _ensure_collection() → _chroma_ready?
    ├── True  → 正常写入 ChromaDB
    └── False → 跳过，静默返回

  query_chunks → _ensure_collection() → _chroma_ready?
    ├── True  → 正常检索
    └── False → 返回空列表 []
```

**关键代码变更**（chroma_store.py）：

```python
_chroma_ready = False
_chroma_init_lock = threading.Lock()

def initialize_chroma() -> None:
    global _client, _collection, _chroma_ready
    Path(CHROMA_PERSIST_DIR).mkdir(parents=True, exist_ok=True)

    def _do_init() -> None:
        global _client, _collection, _chroma_ready
        try:
            with _chroma_init_lock:
                if _chroma_ready:
                    return
                from chromadb import PersistentClient
                _client = PersistentClient(path=str(CHROMA_PERSIST_DIR))
                embedding_fn = _get_chromadb_embedding_function()
                if embedding_fn is None:
                    embedding_fn = _get_embedding_function()
                _collection = _client.get_or_create_collection(
                    name=CHROMA_COLLECTION_NAME,
                    embedding_function=embedding_fn,
                )
                _chroma_ready = True
                logger.info("ChromaDB initialized successfully")
        except Exception:
            logger.warning("ChromaDB init failed, vector store unavailable")

    threading.Thread(target=_do_init, daemon=True, name="chroma-init").start()


def _ensure_collection():
    """获取已初始化的 collection，未就绪返回 None。"""
    if not _chroma_ready:
        return None
    return _collection
```

**效果量化**：

| 指标 | 同步初始化（旧） | 异步初始化（新） | 改善 |
|------|---------------|---------------|------|
| 服务启动时间 | 12-30s（含 ChromaDB） | 1-2s（不含 ChromaDB） | ~10x |
| ChromaDB 失败影响 | 服务启动失败 | 向量功能静默不可用 | 崩溃 → 降级 |
| 分段功能可用性 | 依赖 ChromaDB 成功 | 完全不依赖 ChromaDB | 解耦 |
| 首次模型下载超时 | 导致启动失败 | 后台重试，不影响服务 | 鲁棒性提升 |

---

### 策略预设系统（RAG / 训练 / 摘要三模式）

策略预设系统是研发计划中"策略切换"需求的实现。在7月3日之前，用户需要通过6个参数（min_chars、target_chars、max_chars、heading_flush_min_chars、semantic_boundary_threshold、overlap_sentences）来配置分段行为。对于非专业用户来说，这6个参数的调整门槛较高。

预设系统将参数调优的经验固化到三个预设中：

**预设参数表**：

| 参数 | RAG 检索优化 | 训练语料生产 | 文档摘要 |
|------|------------|------------|---------|
| min_chars | 225 | 500 | 1000 |
| target_chars | 450 | 1000 | 2000 |
| max_chars | 900 | 2000 | 4000 |
| heading_flush_min_chars | 180 | 400 | 800 |
| overlap_sentences | 2 | 0 | 0 |
| semantic_boundary_threshold | 0.50 | 0.55 | 0.60 |
| enable_parent_child | False | True | False |
| 适用场景 | 知识库问答、RAG 检索 | 模型微调、训练数据生产 | 全文总结、文档概览 |
| 设计理念 | 短chunk精准命中 | 长chunk语义完整 | 按章节结构分段 |

**预设设计依据**：

1. **RAG 检索优化（target=450）**：chunk 较短，带有 2 句 overlap。短 chunk 在检索时精准度高（噪声少），overlap 确保相邻 chunk 的上下文不丢失。适用场景是用户提问→检索→回答的知识库问答。

2. **训练语料生产（target=1000）**：chunk 较长，无 overlap。训练数据需要语义完整的文本片段，chunk 间的信息重叠会造成训练数据冗余。还启用了父子块（parent-children），方便训练时灵活选择粒度。

3. **文档摘要（target=2000）**：按章节结构生成大块文本，不切碎。文档摘要需要每个 chunk 覆盖一个完整的主题（通常是一个章节），chunk 间不重叠，确保摘要不重复。

**API 层面的实现**：

```python
# models.py - SegmentConfig 新增方法
@classmethod
def for_preset(cls, preset: str) -> "SegmentConfig":
    presets = {
        "rag": cls(
            min_chars=225, target_chars=450, max_chars=900,
            heading_flush_min_chars=180, overlap_sentences=2,
            enable_semantic_boundary=True,
            semantic_boundary_threshold=0.50,
        ),
        "training": cls(
            min_chars=500, target_chars=1000, max_chars=2000,
            heading_flush_min_chars=400, overlap_sentences=0,
            enable_semantic_boundary=True,
            semantic_boundary_threshold=0.55,
            enable_parent_child=True,
        ),
        "summary": cls(
            min_chars=1000, target_chars=2000, max_chars=4000,
            heading_flush_min_chars=800, overlap_sentences=0,
            enable_semantic_boundary=False,
            semantic_boundary_threshold=0.60,
        ),
    }
    if preset not in presets:
        raise ValueError(f"Unknown preset: {preset}. Available: {list(presets.keys())}")
    return presets[preset]
```

```python
# routes.py - /api/strategies 新增 presets 字段
presets=[
    StrategyPreset(
        name="rag",
        label="RAG 检索优化",
        description="短chunk（目标450字）+2句重叠，精准命中优先，适合知识库问答",
    ),
    StrategyPreset(
        name="training",
        label="训练语料生产",
        description="长chunk（目标1000字）+无重叠，语义完整优先，适合模型微调",
    ),
    StrategyPreset(
        name="summary",
        label="文档摘要",
        description="按章节分段（目标2000字），结构完整优先，适合全文总结",
    ),
],
```

**前端体验**：

在 ConfigPanel.vue 中，预设选择器是一个下拉菜单，用户选择预设后：
- 6个分段参数自动填充对应的预设值
- 参数输入框变为 disabled 状态（灰色不可编辑）
- 选择"自定义"时，参数输入框恢复可编辑状态
- 预设描述以 tooltip 形式展示

---

### 父子块架构

父子块是研发计划中进阶方向的实现。基本思路是：在生成标准 chunk（父块）的同时，对每个父块生成更细粒度的子块。训练时可以根据需要选择粗粒度（父块）或细粒度（子块）。

**数据结构设计**（schemas.py）：

```python
class RetrievedChunk(BaseModel):
    # ... 原有字段 ...
    parent_chunk_id: str = ""       # 所属父块ID（子块非空，父块为空）
    child_chunk_ids: list[str] = Field(default_factory=list)  # 子块ID列表（父块非空，子块为空）
```

**生成逻辑**（segmenter.py）：

```
正常分段 → 得到 N 个 chunk（父块）

如果 enable_parent_child:
  对每个父块：
    1. 按段落拆分为子候选
    2. 确保每个子候选 >= min_child_chars（默认 100 字符）
    3. 子候选过短时与相邻子候选合并
    4. 为每个子块设置 parent_chunk_id
    5. 为父块设置 child_chunk_ids

  输出：
    父块（chunk_type="normal"）+ 子块（chunk_type="normal_child"）
    父块不含原文内容（仅含摘要和元数据），子块含完整原文
```

**使用场景**：
- RAG 检索：使用子块进行检索（精准匹配），父母提供上下文（扩大语义覆盖）
- 训练数据：训练时使用父块（语义完整），评估时使用子块（细粒度评估）
- 当前默认关闭（enable_parent_child=False），作为扩展能力保留

---

### 前端重构与演示页面

前端的重构和演示页面完善是今日王佳杭的核心工作。

**重构前后对比**：

| 维度 | 重构前 | 重构后 |
|------|--------|--------|
| ChunkList.vue 行数 | 580行 | ~300行 |
| 子组件数 | 0（单文件） | 4个（ChunkCard、TagCapsule、EntityBadge、SourceRefPopover） |
| 模板嵌套深度 | 5-6层 | 2-3层 |
| 渲染性能（30 chunk） | ~800ms 首屏 | ~300ms 首屏 |
| 代码可维护性 | 低（逻辑/展示混杂） | 高（展示组件化 + 逻辑独立） |

**子组件职责**：
- ChunkCard：单个 chunk 的展示卡片（标题路径、内容预览、长度标识）
- TagCapsule：标签胶囊组件（颜色编码：关键词=蓝、主题=绿、实体=橙）
- EntityBadge：实体标签组件（类型图标 + 值文本）
- SourceRefPopover：回链气泡组件（点击展开 block_id、page、metadata）

**演示页面四大区域**：

```
┌──────────────────────────────────────────────────────┐
│  配置面板 (ConfigPanel)                               │
│  [预设选择器] [手动参数] [关键词策略] [上传按钮]       │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ┌─────────────────────┐ ┌──────────────────────────┐│
│  │ 分段结果 (ChunkList) │ │ 检索评测 (RetrievalPanel) ││
│  │                     │ │                          ││
│  │ [ChunkCard]         │ │ [查询输入框]              ││
│  │ [ChunkCard]         │ │ [检索模式切换]            ││
│  │ [ChunkCard]         │ │ [Top-K 结果列表]          ││
│  │ ...                 │ │ [相关性分数]              ││
│  └─────────────────────┘ └──────────────────────────┘│
│                                                       │
│  ┌─────────────────────┐ ┌──────────────────────────┐│
│  │ 内容组织 (Organize)  │ │ 指标汇总 (MetricPanel)   ││
│  │                     │ │                          ││
│  │ [标签胶囊列表]       │ │ [Smart vs Heading vs     ││
│  │ [摘要文本]           │ │  Fixed 对比表]           ││
│  │ [实体标签]           │ │ [Recall@k / nDCG / MRR]  ││
│  │ [LLM/规则模式标识]   │ │ [增量分析]                ││
│  └─────────────────────┘ └──────────────────────────┘│
│                                                       │
└──────────────────────────────────────────────────────┘
```

---

### 分段引擎增强与 CLI 工具

今日对分段引擎和 CLI 工具进行了三项增强：

**增强 1 — SegmentConfig.for_preset()**

为 SegmentConfig 新增了 `for_preset()` 类方法，支持三个预设策略。详见 5.2 节。

**增强 2 — segment_file.py 参数扩展**

CLI 工具新增三个参数：
- `--preset`：选择预设策略（rag/training/summary）
- `--parent-child`：启用父子块分段
- `--output-json`：将分段结果输出为 JSON 文件
- `--cache`：缓存分段结果（.pkl 文件），避免重复分段

参数冲突检测：如果同时指定了 --preset 和 --target/--min/--max，CLI 会报错并提示用户二选一。

**增强 3 — SegmentConfig 字段完整列表**

经过阶段5的完善，SegmentConfig 的完整字段列表为：

```python
@dataclass
class SegmentConfig:
    # 长度控制
    min_chars: int = 180
    target_chars: int = 550
    max_chars: int = 800
    heading_flush_min_chars: int = 240

    # 重叠控制
    overlap_sentences: int = 1

    # 语义边界
    enable_semantic_boundary: bool = True
    semantic_boundary_threshold: float = 0.55

    # 关键词策略
    keyword_strategy: str = "tfidf"

    # 进阶功能（M5 新增）
    enable_parent_child: bool = False
    min_child_chars: int = 100

    # 预设策略（M5 新增）
    preset: str | None = None
```

---

###  服务稳定性与错误处理

阶段5对服务稳定性进行了全面的加固：

**加固 1 — 统一错误响应格式**

所有11个 API 端点的错误响应格式统一为：

```json
{
  "error": "ERROR_CODE",
  "message": "人类可读的错误描述",
  "detail": "详细的技术信息（可选）"
}
```

错误码定义：
- `VALIDATION_ERROR`：请求参数校验失败（422）
- `NOT_FOUND`：资源不存在（404）
- `CONFLICT`：资源冲突（409）
- `UNSUPPORTED_FORMAT`：不支持的文档格式（400）
- `SEGMENTATION_FAILED`：分段处理失败（500）
- `CHROMA_UNAVAILABLE`：向量存储不可用（503，但不影响分段）
- `LLM_UNAVAILABLE`：LLM 服务不可用（已降级为规则模式，200 + warning）

**加固 2 — RAG Store 写入重试**

针对高并发场景下 SQLite 的 database is locked 错误，在 ChromaDB 写入操作中增加了重试逻辑：

```python
def upsert_chunks(doc_id: str, chunks: list[dict[str, Any]]) -> None:
    max_retries = 3
    for attempt in range(max_retries):
        try:
            collection = _ensure_collection()
            if collection is None:
                return
            # ... 执行写入 ...
            return
        except Exception as e:
            if "database is locked" in str(e) and attempt < max_retries - 1:
                import time
                time.sleep(0.5 * (attempt + 1))  # 递增等待：0.5s / 1s / 2s
                continue
            raise
```

**加固 3 — 启动流程异常隔离**

main.py 的 startup 事件中，每个初始化步骤都是独立的，一个步骤的失败不影响其他步骤：

```python
@app.on_event("startup")
def on_startup() -> None:
    initialize_model_settings()        # 独立，失败则启动失败（必须）
    initialize_rag_store()             # 独立，SQLite 失败则启动失败
    # ChromaDB 失败不影响启动（异步初始化 + 静默降级）
```

---

### 测试修复与回归验证

阶段5的测试工作确保了代码质量在集成修改后保持稳定。

**修复的测试**：

**测试1：test_model_settings.py**

```
旧代码（mock 路径过期）:
  with patch('backend.app.services.model_settings.get_model_settings') as mock:
      ...

新代码（修正路径）:
  with patch('backend.app.core.model_settings.get_model_settings') as mock:
      ...

根因：7月3日代码重构将 model_settings.py 从 services/ 移至 core/，
      测试中的 mock 路径未同步更新。
```

**测试2：test_segmenting.py**

```
旧代码（断言过严）:
  self.assertLessEqual(statistics["chunk_count"], 20)

新代码（放宽断言）:
  self.assertLessEqual(statistics["chunk_count"], 25)

根因：enable_parent_child 模式下会生成额外的子块 chunk，
      chunk 总数从 ~15 增加到 ~22，超出旧断言上限。
```

**回归验证结果**：

```
============================= test session starts =============================
platform win32 -- Python 3.12.0, pytest-8.x.x

backend/tests/test_segmenting.py::SegmentingSmokeTest:
  test_plain_text_sample_is_chunked_compactly ........ PASSED
  test_docx_sample_keeps_source_refs_complete ........ PASSED  [修复确认]
  test_e2e_segment_with_organizer .................... PASSED
  test_merged_short_sections_keep_first_title_path ... PASSED
  test_metric_chunk_builds_retrieval_text ............ PASSED
  test_recursive_split_handles_long_plain_text ....... PASSED

backend/tests/test_keyword_extraction.py:
  test_extract_tags_basic ............................ PASSED
  test_extract_tags_empty ............................ PASSED
  test_extract_tags_english .......................... PASSED
  test_generate_summary_basic ........................ PASSED
  test_generate_summary_empty ........................ PASSED

backend/tests/test_query_retrieval.py:
  test_local_retrieval_returns_related_chunk ......... PASSED

backend/tests/test_model_settings.py:
  test_initialize_and_update_settings ............... PASSED
  test_initialize_and_update_keep_quality_evaluator .. PASSED  [修复确认]

backend/tests/test_qa_quality_evaluator.py:
  test_answerable_evaluation ......................... PASSED
  test_unanswerable_detection ........................ PASSED
  ...

backend/tests/test_synthesize_qa.py:
  test_synthesize .................................... PASSED
  ...

========================= 27 passed in 28.45s =========================
```

---

### 演示材料准备

今日为 M5 演示准备了完整的材料：

| 材料 | 内容 | 负责人 | 状态 |
|------|------|--------|------|
| 演示脚本 | 5个场景的详细操作步骤 + 讲解要点 | 王佳杭 | 已完成 |
| 演示指南 | README 中的逐步操作说明 | 甘毅 | 已完成 |
| 示例数据 | 3份核心文档 + 2份 benchmark 文档的分段结果 | 王佳杭 | 已完成 |
| 前端界面 | 四大区域的完整展示 | 王佳杭 | 已完成 |
| API 文档 | 11个端点的请求/响应示例 | 于贺 | 已完成 |
| 评测报告 | 三策略对比 Markdown 报告 | 王佳杭 | 已完成（复用7月3日产出） |
| 部署说明 | 一键启动的安装和运行说明 | 于贺 | 初稿完成 |

---

## 问题与解决方案

### 今日新发现的问题

| 编号 | 问题描述 | 严重程度 | 发现人 | 解决方案 | 状态 |
|------|---------|---------|--------|---------|------|
| P-7.6-1 | ChromaDB 并发写入偶发 database is locked（概率约 1/50） | 中 | 于贺 | 增加 retry 逻辑（3次重试 + 递增等待） | 已修复 |
| P-7.6-2 | segment_file.py 中 --preset 和 --target 同时指定时静默忽略 --target | 低 | 孟之语 | 增加参数冲突检测 + 明确的错误提示 | 已修复 |
| P-7.6-3 | ChunkList 在大 chunk 数量（>30）时渲染卡顿 | 低 | 王佳杭 | 使用 v-memo + CSS content-visibility 优化 | 已修复 |
| P-7.6-4 | 演示预演中 ChromaDB 初始化等待时间过长（12s） | 低 | 全组 | 演示前预先启动服务（让 ChromaDB 后台完成初始化） | 已规避 |

### 遗留问题追踪（全量更新）

| 编号 | 问题描述 | 发现日期 | 责任人 | 当前状态 |
|------|---------|---------|--------|---------|
| P-6.28-1 | embedding 模型下载可能失败 | 6月28日 | 孟之语 | 降级方案已就绪（字符3-gram） |
| P-6.29-1 | LLM API 不可用时摘要和标签降级 | 6月29日 | 甘毅 | LLM → 规则降级链路已验证 |
| P-7.1-1 | Smart vs Baseline Recall@5 差距 | 7月1日 | 孟之语、王佳杭 | 已解决：参数调优后 +18.5% |
| P-7.1-2 | heading_based baseline 缺失 | 7月1日 | 孟之语 | 已实现 |
| P-7.2-2 | 评测数据集规模偏小 | 7月2日 | 孟之语 | 已记录：benchmark 10篇文档待标注 |
| P-7.2-4 | eval_rag.py 缺少命令行参数 | 7月2日 | 王佳杭 | 已解决：阶段5已补充 |
| P-7.2-6 | retrieval_text 标题路径权重不足 | 7月2日 | 于贺 | 已记录：后续优化方向 |
| P-7.2-7 | rag_store/ 和 retrieval/ 功能重叠 | 7月2日 | 于贺 | 已记录：长期改进项 |
| P-7.3-1 | EmbeddingStore 多次调用累积 | 7月3日 | 王佳杭 | 已解决：新增 clear_document() |
| P-7.3-2 | 超长文档分段耗时过长 | 7月3日 | 于贺 | 已缓解：新增 --cache 参数 |
| P-7.3-3 | org_suffix 实体召回率偏低 | 7月3日 | 甘毅 | 已修复：后缀词表扩充至18项 |
| P-7.3-4 | metric 正则未覆盖英文全称 | 7月3日 | 甘毅 | 已修复：新增4个英文指标名 |
| P-7.4-1 | ChromaDB 初始化阻塞启动 | 7月4日 | 于贺 | 已解决：异步初始化 + 静默降级 |
| P-7.5-1 | test_model_settings mock 路径过期 | 7月5日 | 孟之语 | 已修复 |
| P-7.5-2 | test_segmenting chunk_count 断言过严 | 7月5日 | 孟之语 | 已修复 |

---

## 下午同步会记录

### 各成员今日完成总结

**孟之语汇报**：

今天主要完成了 M5 验收和 CLI 工具增强。10项验收标准全部通过——服务可启动、11端点可用、4格式全支持、3预设可切换、ChromaDB 降级正常、27个测试全通、前端可用、评测可运行、演示脚本完备、代码已提交。

CLI 工具增加了 --preset、--parent-child、--output-json、--cache 四个参数，功能和参数冲突检测都做好了。

今日工时约 6h。

**于贺汇报**：

今天做了全量回归测试：27个测试 + 11端点 + 3文档端到端，全部通过。服务稳定性验证通过：30分钟连续运行无崩溃，并发10请求无异常。

发现了一个 ChromaDB 并发写入的偶发问题（database is locked），加了 retry 逻辑后解决了。

部署说明文档初稿已写好，明天继续完善。

今日工时约 5.5h。

**甘毅汇报**：

今天完成了 README 中的演示指南章节，5个演示场景的操作步骤都写好了，可以直接复制粘贴运行。内容组织模块的开发者文档也写好了（docs/organizer.md）。

参与了演示预演，负责场景4（内容组织）的演示。预演中发现的问题都记录在案，王佳杭已经修好了。

今日工时约 5h。

**王佳杭汇报**：

今天完成了演示脚本（约800行，5个场景）和前端演示页面的最终完善。ChunkList 重构完成——从580行精简到300行，提取了4个子组件。

演示预演中发现了3个问题（参数锁定、滚动卡顿、ChromaDB等待），已全部修复或规避。

明天开始准备技术报告的第4/5章（评测与对比实验、失败案例分析）。

今日工时约 6h。

### 全组讨论

**讨论 1 — M5 验收正式结论**

孟之语宣读了 M5 验收结果：10/10 验收标准全部通过。M5 里程碑正式验收通过。阶段5（7月4日-6日）三天全部按计划完成。

全组确认：M5 验收通过，可以进入阶段6（文档与报告）。

**讨论 2 — 阶段6（7月7日-9日）的详细安排**

文档日（7月7日）：
- 孟之语：README.md 最终版 + docs/api.md（接口文档）
- 甘毅：docs/data_schema.md（数据格式说明） + docs/organizer.md 完善
- 于贺：docs/deployment.md（部署说明） + docs/quickstart.md（快速开始）
- 王佳杭：技术报告第4/5章初稿

报告日（7月8日）：
- 孟之语：技术报告第1/2/7章
- 甘毅 + 孟之语：技术报告第3章（核心算法设计）
- 王佳杭 + 孟之语：技术报告第4/5章终稿
- 于贺：技术报告第6章（工程实现）

冻结日（7月9日）：
- 全员交叉审阅技术报告
- 最终演示彩排
- 代码/数据/报告冻结
- 提交包整理（README、requirements.txt、示例数据、输出样例、评测结果、技术报告）

**讨论 3 — 技术报告质量要求**

孟之语：技术报告是最终交付的核心文档。按照研发计划，报告需要包含：
1. 课题背景与目标（1-2页）
2. 系统架构设计（2-3页）
3. 核心算法设计（4-6页）—— 这是重点章节
4. 评测与对比实验（3-4页）
5. 失败案例分析（2-3页）
6. 工程实现（1-2页）
7. 总结与展望（1-2页）

总计预估 15-25 页。质量要求：
- 所有评测指标需要有具体的实验数据支撑
- 所有算法设计需要有代码实现支撑（可引用文件路径）
- 所有对比结论需要有分析（不只是数字罗列）
- Badcase 分析需要包含具体的失败案例和改进方向

---

## 组长总结

### 今日整体评价

今天是阶段5（集成测试）的第3天，也是整个项目周期的第15天。全组完成了演示日的全部 10 项任务，M5 里程碑正式验收通过。

**重大成果**：

1. **M5 里程碑验收通过**。10/10 验收标准全部达标。服务、评测、演示材料完成候选版本。从 M1（6月24日）到 M5（7月6日），五个里程碑全部按时通过，没有动用缓冲天数。

2. **ChromaDB 异步初始化**。服务启动时间从 12-30s 降至 1-2s，ChromaDB 故障不影响分段功能。这是项目鲁棒性的重要提升——架构上实现了向量存储与分段引擎的解耦。

3. **策略预设系统**。三个预设（RAG/训练/摘要）固化了参数调优的经验，用户无需手动配置6个参数。预设系统是"策略切换"需求的具体实现，也是演示中的亮点功能。

4. **父子块架构**。虽然默认关闭，但数据结构和生成链路已完整实现。为后续的多粒度检索和训练数据生产提供了架构基础。

5. **前端重构完成**。ChunkList 从580行精简至300行，提取4个子组件。演示页面四大区域数据对接完成，样式统一。

6. **测试全部通过**。27个后端测试 + 11端点 + 3文档端到端 + 4格式支持，全部验证通过。

**好消息汇总**：
- M1-M5 五个里程碑全部按时通过，未动用缓冲
- 服务鲁棒性大幅提升（ChromaDB 解耦、错误处理标准化、写入重试）
- 预设策略为演示增加了"一键切换"的亮点
- 代码质量稳定（27/27 测试通过、48/48 验证项通过）
- 演示材料完备（5场景脚本 + 前端界面 + 演示指南）
- 全组士气良好，准备进入最终文档阶段

**挑战**：
- 阶段6（文档+报告）需要在3天内完成：README、接口文档、数据格式说明、部署说明、技术报告（7章25节）。文档工作量大，需要全组成员高效协作。
- 技术报告是最终交付的核心文档，质量要求高（有实验数据支撑、有代码实现支撑、有深度分析），不能草率完成。
- 距离7月10日最终提交还有4天，时间充足但不可松懈。

### 项目进度对照

按照研发计划，今天是第 15 天。对照阶段 5 计划：

| 计划内容 | 计划状态 | 实际状态 | 偏差分析 |
|---------|---------|---------|---------|
| 7月4日 集成修复日 | FastAPI 联调 + CLI + 日志 + 配置 | 完成 | 符合计划 |
| 7月5日 测试日 | 单元测试 + 接口测试 + 回归 + 抽样 | 完成 | 符合计划 |
| 7月6日 演示日 | 演示页面/HTML报告 + 前端完善 | 完成 | 符合计划 |

对最终交付的影响评估：
- M5 核心验收标准全部通过，不存在降级风险
- 阶段5 三天全部按计划完成，未出现延迟
- 剩余 4 天（7月7-10日）进入阶段6（文档+报告）和最终提交
- 当前进度符合研发计划节奏，关键路径无延迟
- 缓冲天数 3天，未使用

### 组员今日表现

| 成员 | 任务完成 | 工时 | 突出贡献 |
|------|---------|------|---------|
| 孟之语 | 4 项 | 6h | M5 验收报告、CLI 增强、技术报告大纲与分工 |
| 于贺 | 3 项 | 5.5h | 全量回归测试48项通过、并发 retry 修复、部署文档初稿 |
| 甘毅 | 3 项 | 5h | 演示指南编写、内容组织开发者文档、实体识别精度提升验证 |
| 王佳杭 | 4 项 | 6h | 演示脚本800行、前端重构完成、演示预演问题修复 |

全组今日计划完成率 100%。每位成员完成了分配的所有任务。

---

## 关键决策记录

| 编号 | 日期 | 决策内容 | 决策人 | 依据 |
|------|------|---------|--------|------|
| D-7.6-1 | 7月6日 | M5 验收通过，10/10 标准达标，阶段5完成 | 全组 | 10项验收逐项检查全部通过 |
| D-7.6-2 | 7月6日 | 父子块作为"已实现但默认关闭"的进阶功能，演示中简要提及 | 孟之语 | 研发计划中列为进阶方向，架构已完备但非必交付 |
| D-7.6-3 | 7月6日 | 演示采用5场景结构：基础分段→预设策略→检索评测→内容组织→鲁棒性 | 全组 | 由浅入深，每个场景2-3分钟，总计约15分钟 |
| D-7.6-4 | 7月6日 | 7月7日（周一）向老师发送 M1-M5 进度汇报邮件 | 全组 | 研发计划 9.4 节要求里程碑后主动沟通；M4后未及时沟通 |
| D-7.6-5 | 7月6日 | 技术报告撰写分工确认：孟之语1/2/7章、甘毅3章、王佳杭4/5章、于贺6章 | 全组 | 按模块负责分工，7月7-9日完成 |
| D-7.6-6 | 7月6日 | 阶段6不新增任何功能，只做文档、报告、演示彩排和修复 | 全组 | 7月3日决策 D-7.3-5 的延续；临近提交，降低风险 |

---

## 明日计划（7月7日 文档日）

### 明日定位

根据研发计划 4.2 节，7月7日为**文档日**（阶段6第1天）：

> **文档日**：完成 README、接口文档、部署说明、数据格式说明
> **负责人**：于贺（B）、甘毅（C）
> **产出**：文档初版

### 明日任务分配

| 编号 | 任务 | 负责人 | 预计工时 | 优先级 |
|------|------|--------|---------|--------|
| T-12.1 | README.md 最终版：项目简介、快速开始、API列表、评测结果、演示指南、项目结构 | 孟之语 | 3h | P0 |
| T-12.2 | 接口文档 docs/api.md：11个端点的完整请求/响应示例、错误码说明 | 孟之语 | 2h | P0 |
| T-12.3 | 数据格式说明 docs/data_schema.md：DocumentBlock、Chunk、SegmentConfig、OrganizeResult | 甘毅 | 2h | P0 |
| T-12.4 | 部署说明 docs/deployment.md 完善：环境要求、安装步骤、配置说明、常见问题 | 于贺 | 2h | P1 |
| T-12.5 | 快速开始 docs/quickstart.md：从零到运行的5分钟教程 | 于贺 | 1h | P1 |
| T-12.6 | 内容组织文档 docs/organizer.md 完善：RuleBasedTagger + LLMClient 参数说明 | 甘毅 | 1h | P2 |
| T-12.7 | 技术报告第4章 "评测与对比实验" 初稿 | 王佳杭 | 4h | P0 |
| T-12.8 | 技术报告第5章 "失败案例分析" 初稿 | 王佳杭 | 2h | P1 |
| T-12.9 | 向老师发送 M1-M5 进度汇报邮件 | 孟之语 | 0.5h | P1 |

---

## 项目整体进度追踪

### 里程碑完成情况

| 里程碑 | 内容 | 计划日期 | 实际日期 | 状态 |
|--------|------|---------|---------|------|
| M1 | 分段引擎初版可用 | 6月24日 | 6月24日 | 通过 |
| M2 | PDF/DOCX + 图片提取 + 预处理 | 6月27日 | 6月27日 | 通过 |
| M3 | 核心智能体闭环初步可用 | 6月30日 | 6月30日 | 通过 |
| M4 | 能输出对比实验结果 | 7月3日 | 7月3日 | 通过 |
| M5 | 集成修复完成 + 演示可用 | 7月6日 | 7月6日 | 通过 |
| M6 | 最终可交付版本 | 7月9日 | — | 待进行 |
| M7 | 最终提交与验收展示 | 7月10日 | — | 待进行 |

### 阶段完成情况

| 阶段 | 内容 | 日期 | 状态 |
|------|------|------|------|
| 阶段0 | 选题确认与开题准备 | 6月22日 | 已完成 |
| 阶段1 | 架构、schema、接口、样例数据 | 6月23-24日 | 已完成 |
| 阶段2 | 基础分段与回链 | 6月25-27日 | 已完成 |
| 阶段3 | 语义增强、特殊块保护、内容组织初版 | 6月28-30日 | 已完成 |
| 阶段4 | baseline、检索评测、指标报告 | 7月1-3日 | 已完成 |
| 阶段5 | 集成测试、前端/报告、失败案例 | 7月4-6日 | 已完成 |
| 阶段6 | 文档、答辩材料、演示彩排、缓冲修复 | 7月7-9日 | — 当前 |
| 阶段7 | 最终提交与展示 | 7月10日 | 待进行 |

### 缓冲消耗情况

- 总缓冲天数：3天（研发计划预留）
- 已消耗：0天
- 剩余：3天
- 当前进度与计划一致，未动用缓冲

---

## 任务状态表

### 阶段5 任务完成情况（7月4日-6日）

| 日期 | 计划任务数 | 完成数 | 完成率 |
|------|-----------|--------|--------|
| 7月4日 | 8 | 8 | 100% |
| 7月5日 | 8 | 8 | 100% |
| 7月6日 | 10 | 10 | 100% |
| **合计** | **26** | **26** | **100%** |

### 全项目任务完成情况（截至7月6日）

| 阶段 | 计划任务数 | 完成数 | 完成率 |
|------|-----------|--------|--------|
| 阶段1 | 6 | 6 | 100% |
| 阶段2 | 12 | 12 | 100% |
| 阶段3 | 15 | 15 | 100% |
| 阶段4 | 24 | 24 | 100% |
| 阶段5 | 26 | 26 | 100% |
| **合计** | **83** | **83** | **100%** |

---

## 里程碑验收状态

### 已通过里程碑汇总

| 里程碑 | 日期 | 验收项数 | 通过率 |
|--------|------|---------|--------|
| M1 | 6月24日 | 5 | 100% |
| M2 | 6月27日 | 6 | 100% |
| M3 | 6月30日 | 10 | 100% |
| M4 | 7月3日 | 10 | 100% |
| M5 | 7月6日 | 10 | 100% |

---

## 阶段5完整工作总结

阶段5（7月4日-6日）是整个项目周期中"承前启后"的阶段——在M4评测闭环的基础上，进行服务集成、测试修复和演示准备，为阶段6的文档撰写和最终交付打好基础。

### 阶段5核心产出汇总

**服务层面**：

| 产出 | 说明 | 影响 |
|------|------|------|
| ChromaDB 异步初始化 | 后台线程初始化，不阻塞启动 | 启动时间 -10x，鲁棒性 +100% |
| 统一错误响应格式 | 7种错误码 + 标准JSON响应 | API 可用性提升 |
| RAG Store 写入重试 | 3次递增等待重试 | 并发写入成功率 98% → 100% |
| 启动流程异常隔离 | 各初始化步骤独立 | 单个组件失败不影响整体 |

**功能层面**：

| 产出 | 说明 | 影响 |
|------|------|------|
| 策略预设系统 | RAG/训练/摘要 三个预设 | 用户操作从6参数降至1选择 |
| 父子块架构 | Parent-Child 数据结构 + 生成链路 | 为多粒度检索提供基础 |
| SegmentConfig.for_preset() | 预设策略工厂方法 | 代码复用 + API 统一 |
| CLI 工具增强 | --preset、--parent-child、--output-json、--cache | 命令行可用性提升 |

**前端层面**：

| 产出 | 说明 | 影响 |
|------|------|------|
| ChunkList 重构 | 580行 → 300行 + 4个子组件 | 可维护性 + 渲染性能提升 |
| 预设选择器 | 预设下拉 + 参数锁定 | 用户体验提升 |
| 统一错误拦截器 | API 错误友好提示 | 用户体验提升 |
| 演示页面完善 | 四大区域数据对接 | 演示就绪 |

**质量层面**：

| 产出 | 说明 | 影响 |
|------|------|------|
| 测试修复 | 2个失败 → 27/27全部通过 | 代码质量保证 |
| 全量回归 | 48项验证全部通过 | 端到端质量保证 |
| 人工抽样 | 9 chunk 质量检查通过率 88.9% | 内容组织质量基线 |
| 服务稳定性验证 | 30分钟无崩溃 + 并发无异常 | 生产就绪 |

**文档层面**：

| 产出 | 说明 | 影响 |
|------|------|------|
| 演示脚本 | 5场景800行 | 演示标准化 |
| 演示指南 | README 新增章节 | 可复现演示 |
| 部署说明 | docs/deployment.md 初稿 | 环境搭建标准化 |
| 开发者文档 | docs/organizer.md | 模块可维护性 |

### 阶段5质量指标总结

| 指标类别 | 指标 | 值 | 判定 |
|---------|------|-----|------|
| 测试 | 后端单元测试通过率 | 27/27 = 100% | 达标 |
| API | 端点可用率 | 11/11 = 100% | 达标 |
| 端到端 | 格式支持率 | 4/4 = 100% | 达标 |
| 服务 | 启动时间 | < 2s | 达标 |
| 服务 | 稳定性（30分钟） | 0 崩溃 | 达标 |
| 内容组织 | 标签准确率 | 0.70 (Jaccard) | 达标 |
| 内容组织 | 摘要忠实度 | 100% | 达标 |
| 内容组织 | 实体精确率 | 95.9% → 96.8% | 提升 |
| 内容组织 | 实体召回率 | 85.5% → 93.0% | 提升 |
| 评测 | Recall@5 提升 | +18.5% (vs Fixed) | 超标 |

---

## M5验收报告

**验收日期**：2026年7月6日
**验收人**：孟之语、于贺
**验收结论**：通过

| # | 验收项 | 具体标准 | 验证方法 | 结果 |
|---|--------|---------|---------|------|
| 1 | 服务可启动 | 一条命令启动，/health 正常响应 | `uvicorn backend.app.main:app` → `curl /health` | 通过 |
| 2 | 11端点可用 | 所有端点有正常请求/响应，错误处理完善 | curl 逐一测试 | 通过 |
| 3 | 分段全链路 | 4种格式文档上传→分段→入库→检索 | 上传4份测试文档 | 通过 |
| 4 | 预设策略可用 | RAG/训练/摘要三个预设可正常切换 | 同一文档三个预设分别分段，chunk数量不同 | 通过 |
| 5 | ChromaDB 降级 | ChromaDB 不可用时服务不崩溃 | 模拟 chromadb 缺失 | 通过 |
| 6 | 27个测试全通 | `pytest backend/tests/ -v` 全部通过 | 运行测试套件 | 通过 |
| 7 | 前端演示可用 | 分段/检索/组织/指标四大区域正常渲染 | 浏览器访问 | 通过 |
| 8 | 评测可运行 | eval_rag.py + eval_report.py 正常输出 | 运行评测脚本 | 通过 |
| 9 | 演示脚本完备 | 5个演示场景的操作流程清晰可执行 | 全组预演 | 通过 |
| 10 | 代码已提交 | 所有改动已纳入版本管理 | `git status` | 通过 |

**验收签名**：

```
M5 里程碑验收通过

验收标准：10/10 全部达标
服务状态：可启动、可运行、可演示
测试状态：27/27 全部通过
API 端点：11/11 全部到齐
降级机制：ChromaDB 异步初始化 + 静默降级
预设策略：RAG/训练/摘要 三种预设正常工作
前端状态：四大展示区域数据对接完成

验收人：孟之语
日期：2026年7月6日
```

---

## 技术报告大纲与分工确认

### 技术报告大纲（最终版）

```
课题11：面向RAG的智能分段与内容组织智能体
              —— 技术报告

1. 课题背景与目标（负责人：孟之语）
  1.1 课题11定位与任务书要求
  1.2 RAG文档预处理的痛点
  1.3 智能分段与内容组织的必要性
  1.4 交付物清单

2. 系统架构设计（负责人：孟之语）
  2.1 总体架构
  2.2 数据结构设计（DocumentBlock、Chunk、SegmentConfig）
  2.3 API 接口设计（11个端点）
  2.4 技术栈选型与降级策略

3. 核心算法设计（负责人：甘毅 + 孟之语）
  3.1 分段流程总览
  3.2 标题边界优先策略
  3.3 语义边界增强（embedding 相似度 + 边界评分）
  3.4 特殊块保护（表格/公式/代码/列表/图片）
  3.5 长度控制与 overlap 机制
  3.6 内容组织（关键词/摘要/实体/标签）
  3.7 回链与元数据（title_path、source_refs、strategy_info、quality_flags）
  3.8 自适应参数与策略预设

4. 评测与对比实验（负责人：王佳杭 + 孟之语）
  4.1 评测框架设计
  4.2 Baseline 策略（fixed_length、heading_based）
  4.3 评测数据集
  4.4 IR 指标（Recall@k、Precision@k、nDCG@k、MRR）
  4.5 分段质量统计（不破句率、表格成块率、长度命中率、回链完整率）
  4.6 内容组织质量评测（标签准确率、摘要忠实度、实体精度）
  4.7 三策略对比结果与分析
  4.8 参数调优实验与最优配置

5. 失败案例分析（负责人：王佳杭）
  5.1 Badcase 分类与统计
  5.2 典型失败模式分析
  5.3 改进方向与后续工作

6. 工程实现（负责人：于贺）
  6.1 服务集成（FastAPI + Uvicorn + ChromaDB）
  6.2 前端展示（Vue 3 + Vite + marked）
  6.3 CLI 工具与脚本
  6.4 部署说明与环境要求

7. 总结与展望（负责人：孟之语）
  7.1 课题完成情况
  7.2 技术亮点与创新点
  7.3 已知限制与改进方向
  7.4 项目经验与教训
```

### 工作安排

| 日期 | 负责人 | 交付内容 | 预计页数 |
|------|--------|---------|---------|
| 7月7日 | 孟之语 | 第1/2章初稿 | 3-5页 |
| 7月7日 | 王佳杭 | 第4/5章初稿 | 6-8页 |
| 7月8日 | 甘毅 + 孟之语 | 第3章 | 4-6页 |
| 7月8日 | 王佳杭 + 孟之语 | 第4/5章终稿 | 6-8页 |
| 7月8日 | 于贺 | 第6章 | 1-2页 |
| 7月8日 | 孟之语 | 第7章 + 全文统稿 | 1-2页 |
| 7月9日 | 全员 | 交叉审阅 + 修改 | — |

---

## 演示脚本与演示场景

### 演示场景总览

| 场景 | 名称 | 时长 | 负责人 | 核心展示点 |
|------|------|------|--------|-----------|
| 1 | 基础分段演示 | 3分钟 | 孟之语 | 文档上传→分段→chunk元数据→回链 |
| 2 | 预设策略演示 | 3分钟 | 王佳杭 | 同一文档三种预设的分段效果对比 |
| 3 | 检索评测演示 | 4分钟 | 王佳杭 | 三策略对比指标表→增量分析→badcase |
| 4 | 内容组织演示 | 3分钟 | 甘毅 | 标签/摘要/实体→LLM增强→降级 |
| 5 | 鲁棒性演示 | 2分钟 | 于贺 | ChromaDB降级→错误处理→并发 |

**总计**：约 15 分钟

### 场景1：基础分段演示（详细脚本）

```
[操作] 启动后端服务
  python -m uvicorn backend.app.main:app --host 0.0.0.0 --port 8000

[操作] 启动前端
  cd frontend && npm run dev

[操作] 浏览器打开 http://localhost:5173

[操作] 上传文档 assets/title.md
  - 点击"上传文档"按钮
  - 选择 assets/title.md
  - 点击"开始分段"

[演示] 分段结果展示
  - 点击任意 chunk 卡片，展开详情
  - 查看 chunk_id、title_path、char_count、token_count
  - 查看标签胶囊（关键词/主题标签）
  - 查看摘要文本
  - 查看 source_refs 回链信息（block_id、page）

[讲解词]
"这是一篇任务书文档，系统将其分为了5个语义完整的chunk。
每个chunk都包含了完整的元数据。
标题路径显示了chunk在文档结构中的位置。
标签是自动提取的关键词——不需要人工标注。
摘要保证100%忠实——因为我们使用的是抽取式方法，
所有文字都来自原文。
回链可以追溯到文档中的具体段落和页码，
确保信息可核查。"
```

### 场景2：预设策略演示（详细脚本）

```
[操作] 上传同一文档 assets/调研报告.docx，使用"RAG检索优化"预设
  - 在配置面板中选择预设："RAG 检索优化"
  - 观察参数自动填充：target=450, overlap=2
  - 点击"开始分段"
  - 记录 chunk 数量：~38 个

[操作] 使用"训练语料生产"预设
  - 选择预设："训练语料生产"
  - 参数自动更新：target=1000, overlap=0
  - 勾选"启用父子块"
  - 点击"开始分段"
  - 记录 chunk 数量：~18 个

[操作] 使用"文档摘要"预设
  - 选择预设："文档摘要"
  - 参数自动更新：target=2000, overlap=0
  - 点击"开始分段"
  - 记录 chunk 数量：~8 个

[讲解词]
"同一篇调研报告，三种不同的分段策略。
RAG预设：38个短chunk，每个约450字，带句子重叠，
适合知识库问答——用户提问后快速精准命中。
训练预设：18个长chunk，每个约1000字，无重叠，
语义完整，适合作为模型微调的训练语料。
摘要预设：8个大chunk，按章节结构分段，
每个chunk覆盖一个完整主题，适合全文总结。
一键切换策略，无需手动调整6个参数。"
```

### 场景3：检索评测演示（详细脚本）

```
[操作] 运行评测脚本
  python scripts/eval_rag.py

[操作] 展示终端输出
  三策略对比指标表：
  | 指标     | Smart   | Heading | Fixed   | S vs F  |
  |----------|---------|---------|---------|---------|
  | Recall@1 | 0.1669  | 0.1534  | 0.1456  | +14.6%  |
  | Recall@3 | 0.4042  | 0.3895  | 0.3005  | +34.5%  |
  | Recall@5 | 0.5915  | 0.5433  | 0.4990  | +18.5%  |
  | nDCG@5   | 0.7148  | 0.6568  | 0.6026  | +18.6%  |

[操作] 运行评测报告生成
  python scripts/eval_report.py
  cat data/outputs/evaluation_report.md

[讲解词]
"我们的智能分段策略（Smart）相比固定长度基线（Fixed），
在Recall@5上提升了18.5%，超过了任务书要求的+10%目标。

这个提升来自两个独立贡献：
结构信息贡献（Heading vs Fixed）：+8.9%
语义边界增量（Smart vs Heading）：+8.8%
两者相加约等于总提升+18.5%，
说明结构感知和语义边界是两个独立且互补的增强维度。

评测报告还包含了badcase分析：
15个问题中识别出4个badcase，
其中Fixed策略占了3个——验证了固定长度分段
在跨标题信息检索上的天然劣势。"
```

### 场景4：内容组织演示（详细脚本）

```
[操作] 调用内容组织API
  curl -X POST http://localhost:8000/api/organize \
    -H "Content-Type: application/json" \
    -d '{
      "doc_id": "my_doc",
      "chunks": [
        {"chunk_id": "c1", "content": "面向RAG的智能分段系统..."},
        {"chunk_id": "c2", "content": "分段策略包括标题边界优先..."}
      ]
    }'

[操作] 查看每个chunk的标签/摘要/实体
  - tags: ["智能分段", "RAG", "标题边界", ...]
  - summary: "该系统通过标题边界优先策略..."
  - entity_labels: [{"type": "metric", "value": "Recall@5"}, ...]

[操作] 设置API Key，展示LLM增强模式
  export OPENAI_API_KEY=sk-xxx
  # 重新调用 /api/organize
  - tags 质量明显提升（更多维度的标签）
  - summary 更加精炼和全面

[操作] 取消API Key，展示自动降级
  unset OPENAI_API_KEY
  # 重新调用 /api/organize
  - 服务正常返回（不报错）
  - 自动回退到规则模式
  - quality_flags 中包含 "rule_mode" 标识

[讲解词]
"内容组织模块为每个chunk自动生成标签、摘要和实体。
规则模式使用jieba分词+TF-IDF+正则表达式，
标签准确率约70%，摘要忠实度100%。
LLM模式使用DeepSeek大模型，
标签准确率可达98%，但需要API Key。
最关键的是：即使API Key不可用，
系统会自动降级为规则模式，服务不中断。"
```

### 场景5：鲁棒性演示（详细脚本）

```
[操作] 展示服务启动日志
  startup: model settings initialized
  startup: initializing rag store (SQLite + ChromaDB)
  startup: rag store ready
  ChromaDB initialized successfully (后台线程，约12秒后)

[操作] ChromaDB未就绪时执行分段
  curl -X POST http://localhost:8000/api/segment/upload \
    -F "file=@assets/title.md"
  # 正常返回分段结果（不依赖ChromaDB）

[操作] ChromaDB未就绪时执行检索
  curl -X POST http://localhost:8000/api/query \
    -H "Content-Type: application/json" \
    -d '{"doc_id": "title_md", "query": "什么是智能分段", "top_k": 5}'
  # 向量检索返回空（ChromaDB未就绪）
  # SQLite检索正常返回结果

[操作] 错误处理演示
  # 无效格式
  curl -X POST http://localhost:8000/api/segment/upload \
    -F "file=@test.exe"
  → {"error": "UNSUPPORTED_FORMAT", "message": "不支持的文件格式: .exe"}

  # 不存在的文档
  curl http://localhost:8000/api/chunks/nonexistent_doc
  → {"error": "NOT_FOUND", "message": "文档不存在: nonexistent_doc"}

[讲解词]
"系统的鲁棒性设计体现在三个层面：
1. 向量存储与分段引擎解耦——
  ChromaDB不可用时，分段功能不受影响。
2. 异步初始化——
  ChromaDB在后台初始化，不阻塞服务启动。
3. 统一的错误响应格式——
  所有API错误返回结构化的JSON，
  前端可以统一处理，用户看到友好的提示。
这是为生产环境设计的降级机制。"
```

---

## 风险评估与缓冲消耗

### 当前风险矩阵

| 风险编号 | 风险描述 | 类别 | 可能性 | 影响 | 缓解措施 | 当前状态 |
|---------|---------|------|--------|------|---------|---------|
| R-1 | 技术报告质量不达标 | 文档 | 中 | 高 | 分章节负责人 + 交叉审阅 | 阶段6关注 |
| R-2 | 演示时环境问题 | 演示 | 低 | 中 | 预演 + 降级方案 | 已预演 |
| R-3 | 评测指标在老师环境中无法复现 | 评测 | 低 | 高 | 固化依赖版本 + requirements.txt | 已记录 |
| R-4 | 文档未按时完成 | 进度 | 低 | 中 | 3天文档时间 + 1天缓冲 | 已计划 |
| R-5 | 前端在演示时渲染异常 | 演示 | 低 | 中 | 预演 + curl 降级演示 | 已预演 |

### 缓冲消耗情况

- 总缓冲天数：3天
- 已消耗：0天
- 剩余：3天

### 降级方案

如果在阶段6出现意外（如文档工作量超出预期、演示彩排发现阻塞问题），按以下优先级降级：

| 优先级 | 内容 | 降级方案 |
|--------|------|---------|
| P0（不可降级） | README、接口文档、技术报告 | 必须完成 |
| P1（可精简） | 数据格式说明、部署说明 | 合并到 README |
| P2（可延后） | 开发者文档、演示指南 | 提交后补充 |
| P3（可选） | 父子块演示、预设策略对比评测 | 不演示 |

---

## 附录：关键代码与数据

### SegmentConfig 最终字段列表

```python
@dataclass
class SegmentConfig:
    # 长度控制
    min_chars: int = 180
    target_chars: int = 550
    max_chars: int = 800
    heading_flush_min_chars: int = 240

    # 重叠控制
    overlap_sentences: int = 1

    # 语义边界
    enable_semantic_boundary: bool = True
    semantic_boundary_threshold: float = 0.55

    # 关键词策略
    keyword_strategy: str = "tfidf"

    # 进阶功能（M5 新增）
    enable_parent_child: bool = False
    min_child_chars: int = 100

    # 预设策略（M5 新增）
    preset: str | None = None

    @classmethod
    def auto(cls, total_chars: int) -> "SegmentConfig":
        """根据文档总长度自适应选择参数（5档）"""
        ...

    @classmethod
    def for_preset(cls, preset: str) -> "SegmentConfig":
        """根据预设名称返回配置（rag/training/summary）"""
        ...
```

### API 端点完整列表（最终版）

| # | 端点 | 方法 | 功能 | 新增/变更 |
|---|------|------|------|----------|
| 1 | /health | GET | 健康检查 | — |
| 2 | /api/segment/upload | POST | 文档上传 → 分段 → 入库 | 新增 preset/parent_child 参数 |
| 3 | /api/chunks/all | GET | 列出所有 chunk | — |
| 4 | /api/chunks/{doc_id} | GET | 按文档获取 chunk | — |
| 5 | /api/query | POST | RAG 检索查询 | — |
| 6 | /api/synthesize-qa | POST | QA 对合成 | — |
| 7 | /api/model-settings | GET/PUT | LLM 配置管理 | — |
| 8 | /api/images/{doc_id} | GET | 文档图片列表 | — |
| 9 | /api/strategies | GET | 可用策略列表 | 新增 presets 字段 |
| 10 | /api/organize | POST | 独立内容组织 | — |
| 11 | /api/evaluate | POST | 三策略对比评测 | — |

### 策略预设参数对照表

| 参数 | RAG 检索优化 | 训练语料生产 | 文档摘要 |
|------|------------|------------|---------|
| min_chars | 225 | 500 | 1000 |
| target_chars | 450 | 1000 | 2000 |
| max_chars | 900 | 2000 | 4000 |
| heading_flush_min_chars | 180 | 400 | 800 |
| overlap_sentences | 2 | 0 | 0 |
| semantic_boundary_threshold | 0.50 | 0.55 | 0.60 |
| enable_parent_child | False | True | False |

### 评测指标总表（默认参数，最终确认版）

| 指标 | Smart | Heading | Fixed | S vs F |
|------|-------|---------|-------|--------|
| Recall@1 | 0.1669 | 0.1534 | 0.1456 | +14.6% |
| Recall@3 | 0.4042 | 0.3895 | 0.3005 | +34.5% |
| Recall@5 | 0.5915 | 0.5433 | 0.4990 | **+18.5%** |
| Precision@5 | 0.6033 | 0.5567 | 0.5233 | +15.3% |
| nDCG@5 | 0.7148 | 0.6568 | 0.6026 | +18.6% |
| MRR | 0.7778 | 0.7500 | 0.7500 | +3.7% |

结构信息贡献 (Heading vs Fixed): **+8.9%**
语义边界增量 (Smart vs Heading): **+8.8%**
总提升 (Smart vs Fixed): **+18.5%**

### 分段质量统计

| 指标 | 目标 | 实测（3文档加权平均） | 判定 |
|------|------|---------------------|------|
| 不破句率 | 100% | 100% | 达标 |
| 表格/公式/代码整体成块率 | >= 95% | 100% | 达标 |
| 目标长度区间命中率 | >= 90% | 94.4% | 达标 |
| 原文回链完整率 | 100% | 100% | 达标 |

### 内容组织质量基线（最终版）

| 指标 | 值 | 目标 | 判定 |
|------|-----|------|------|
| 标签准确率（词级Jaccard 0.5） | 0.70 | >= 0.5 | 达标 |
| 摘要忠实度 | 100% | >= 90% | 达标 |
| 实体精确率（规则模式） | 96.8% | 报告数值 | — |
| 实体召回率（规则模式） | 93.0% | 报告数值 | — |

### 系统默认参数（最终确认版）

```
min_chars:                   180
target_chars:                550
max_chars:                   800
heading_flush_min_chars:     240
overlap_sentences:           1
enable_semantic_boundary:    True
semantic_boundary_threshold: 0.55
keyword_strategy:            tfidf
enable_parent_child:         False
```

### 项目文件结构（M5最终版）

```
zhinengti/
  README.md                          # 项目说明 + 快速开始 + 演示指南
  requirements.txt                   # Python 依赖
  backend/
    app/
      main.py                        # FastAPI 入口
      api/
        routes.py                    # 11个API端点
      core/
        config.py                    # 配置常量
        model_settings.py            # LLM 配置管理
      models/
        schemas.py                   # Pydantic 数据模型
      services/
        document_loader/             # 多格式文档加载
        document_preprocessor.py     # 文档预处理
        embedding.py                 # MiniLM embedding
        evaluation/                  # 评测框架（baseline + metrics）
        organizer/                   # 内容组织（RuleBasedTagger + LLMClient）
        preprocessing/               # 封面/目录去除
        rag_store/                   # RAG 存储（SQLite + ChromaDB）
        retrieval/                   # 检索服务（EmbeddingStore + TF-IDF）
        segmenting/                  # 核心分段引擎
    tests/                           # 27个单元测试
  frontend/
    src/
      api/                           # API 调用封装
      components/                    # Vue 组件
      views/                         # 页面视图
  scripts/                           # CLI工具 + 评测脚本
  data/
    benchmarks/                      # 10篇评测基准文档
    images/                          # 提取的文档图片
    outputs/                         # 评测报告输出
  docs/                              # 文档目录（阶段6填充）
```

---

*报告撰写：孟之语*
*数据核实：王佳杭、于贺*
*审核：全组*
