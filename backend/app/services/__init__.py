"""Services layer.

Sub-packages:
    document_loader/   — Multi-format document loading (txt, md, docx, pdf)
    preprocessing/     — Cover/TOC removal, table flattening
    segmenting/        — Core segmentation engine
    retrieval/         — TF-IDF and embedding-based vector stores
    evaluation/        — RAG evaluation framework (baseline, metrics)
"""
